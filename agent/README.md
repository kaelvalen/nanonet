# NanoNet Agent

Rust monitoring agent. Dokümantasyon: [../docs/agent.md](../docs/agent.md).

```bash
cargo build --release
cargo test
make agent    # kök Makefile, .env ile
```

Kurulum: `install.sh`, `../agent-setup.sh`.
